---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: green-name-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: green-name-app
  template:
    metadata:
      labels:
        app: green-name-app
    spec:
      containers:
        - name: green-name-app
          image: iamsicher/nameapp:green #change image
          ports:
            - containerPort: 80
          env:
            - name: MYSQL_HOST
              value: mysql
            - name: MYSQL_USER
              value: root
            - name: MYSQL_PASSWORD
              value: example123
            - name: MYSQL_DATABASE
              value: namedb
---
apiVersion: v1
kind: Service
metadata:
  name: green-name-app-svc
spec:
  selector:
    app: green-name-app
  type: LoadBalancer # 修改为 LoadBalancer
  ports:
    - port: 4444 # 集群内部访问端口
      targetPort: 80 # Pod 监听端口
      protocol: TCP
